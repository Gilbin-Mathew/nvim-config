# My custom nvim config

### just a config for my neovim.

Just configured because i got fed up with switching colorschemes, and to know much about the config of nvim, not much though.

>[!NOTE]
> treesitters only works for c by default
> if needed manually install parsers.so of 
> particluar language on nvim runtime treesitters directory
